# Test Credentials

## Admin Panel
- URL: /admin
- Password: hamoude2024
(Password constant in `src/lib/carStore.ts` → ADMIN_PASSWORD; session-based auth via sessionStorage key `hamoude_admin_auth`.)

## Supabase
- URL/anon key: see /app/netlify.toml (build env) and /app/.env.local (local dev, gitignored).
